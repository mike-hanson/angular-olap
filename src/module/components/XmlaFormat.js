(function (window) {
    'use strict';

    var olap = (window.olap = window.olap || {});
    olap.XmlaFormat = {
        Tabular: 'Tabular',
        Multidimensional: 'Multidimensional'
    }
})(window);