(function (window) {
    'use strict';

    var olap = (window.olap = window.olap || {});

    olap.XmlaStructureType = {
        Unknown: 'Unknown',
        Unnatural: 'Unnatural',
        Natural: 'Natural'
    };
})(window);